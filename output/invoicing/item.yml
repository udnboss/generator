store:
  id: string
  name: string    
  category_id?: string > category.id

view:
  id: string
  name: string
  category_id?: string > category.id
  category?: =category

create:
  id: string
  name: string
  category_id?: string > category.id

update:
  id?: string
  name?: string
  category_id?: string > category.id 

delete:
  id: string 
  category_id?: string > category.id 

filter:
  id?: string
  name?: string
  category_id?: string > category.id

sort:
  id?: string
  name?: string
  category_id?: string > category.id   